<!-- resources/mapper/UserInfMapper.xml -->
<mapper namespace="com.example.demo.mapper.UserInfMapper">

    <select id="countByIdAndPassword" resultType="int">
        SELECT COUNT(*) 
        FROM userInf
        WHERE id = #{userId} 
          AND password = #{password}
    </select>

</mapper>
